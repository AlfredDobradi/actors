info:
  name: Admin - Get Accounts
  type: http

http:
  method: GET
  url: http://127.0.0.1:8080/admin/accounts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
